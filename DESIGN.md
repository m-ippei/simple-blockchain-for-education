# 設計書 - 学習用ブロックチェーン実装

## 📋 目次

1. [要件定義](#1-要件定義)
2. [システム設計](#2-システム設計)
3. [モジュール詳細設計](#3-モジュール詳細設計)
4. [データ設計](#4-データ設計)
5. [UI設計](#5-ui設計)

---

## 1. 要件定義

### 1.1 目的

Pythonコードを通じてブロックチェーンの最小構造を理解するための学習用アプリケーションを実装する。

**学習目標:**
- トランザクションとは何か
- ブロックとは何か
- ハッシュの役割
- previous_hash による連鎖構造
- 改ざん検知の仕組み

**非目標:**
実運用レベルの安全性・分散性は対象外

### 1.2 スコープ

#### 対象

✅ CLIベースアプリケーション  
✅ ファイル保存型ローカルブロックチェーン  
✅ 参加者3〜4名固定  
✅ 簡易Proof of Work  
✅ 改ざん検知機能

#### 非対象

❌ 公開鍵暗号・電子署名  
❌ P2Pネットワーク  
❌ トークン経済設計  
❌ 本格的な暗号耐性  
❌ マークルツリー  
❌ UTXOモデル

### 1.3 機能要件

#### F1. 参加者管理

- 固定参加者をJSONで管理
- アドレスは3〜5文字
- 存在チェック可能
- 一覧表示可能

#### F2. トランザクション管理

**データ項目:**
- from: 送信元アドレス
- to: 送信先アドレス
- amount: 金額（正の整数）
- timestamp: UNIXタイム

**機能:**
- 対話的作成
- ファイル保存（transaction-NNNN.json）
- 未処理一覧取得
- バリデーション

**バリデーション:**
- from/to が登録済みアドレス
- amount が正の整数
- from ≠ to

#### F3. ブロック生成

**データ項目:**
- index: ブロック番号
- timestamp: 作成時刻
- transactions: トランザクションリスト
- previous_hash: 前のブロックのハッシュ
- nonce: Proof of Work用
- hash: このブロックのハッシュ

**機能:**
- ブロック生成
- ハッシュ計算（SHA256）
- JSON変換・復元

#### F4. 簡易Proof of Work

**仕様:**
- ハッシュアルゴリズム: SHA256
- 難易度: 先頭"00"一致（固定）
- nonce探索
- 進行状況表示（1000回ごと）

#### F5. チェーン管理

**機能:**
- ブロック追加
- ブロック読み込み
- チェーン検証
- 状態取得

**ファイル形式:**
- block-NNNN.json
- index順に保存

#### F6. 検証機能

**検証項目:**
1. ハッシュ再計算一致
2. previous_hash 整合性
3. index 連続性

**出力:**
- 検証成功時: OK メッセージ
- 検証失敗時: 破損箇所明示

#### F7. CLI操作

**起動時表示:**
- 現在のブロック数
- 未処理トランザクション数

**メニュー:**
1. 参加者一覧表示
2. トランザクション作成
3. 未処理トランザクション表示
4. マイニング実行
5. チェーン表示
6. チェーン検証
7. ブロック改ざん（学習用）
8. 終了

### 1.4 非機能要件

#### NFR1. 可読性

- コードは初心者が理解できること
- docstring 必須
- コメントは丁寧に

#### NFR2. 保守性

- 1ファイル1責務
- 関数は短く（50行以内推奨）
- 明示的な命名

#### NFR3. テスタビリティ

- 各モジュール単体実行可能
- テストコード付属
- エラーメッセージ明確

#### NFR4. ユーザビリティ

- CLI操作が直感的
- 視覚的な区切り線
- 進行状況表示

### 1.5 成功基準

✅ ブロックを改ざんすると検証が失敗する  
✅ nonce探索が視覚的に確認できる  
✅ 初心者がCLIだけで仕組みを追体験できる

---

## 2. システム設計

### 2.1 ディレクトリ構成

```
simple-blockchain/
├── README.md                      # プロジェクト概要
├── AGENTS.md                      # 実装者向けガイド
├── DESIGN.md                      # この文書
├── instructions/                  # 実装指示書
│   ├── 01-SETUP.md
│   ├── 02-PARTICIPANTS-TRANSACTION.md
│   ├── 03-BLOCK-MINER.md
│   ├── 04-BLOCKCHAIN.md
│   └── 05-CLI-INTEGRATION.md
├── src/
│   └── simple_blockchain/
│       ├── __init__.py
│       ├── participants.py
│       ├── transaction.py
│       ├── block.py
│       ├── miner.py
│       ├── blockchain.py
│       └── cli.py
└── data/
    ├── participants.json
    ├── transactions/
    │   ├── transaction-0001.json
    │   └── ...
    └── blocks/
        ├── block-0000.json
        └── ...
```

### 2.2 モジュール構成

| モジュール | 責務 | 依存 |
|-----------|------|------|
| participants.py | 参加者管理 | なし |
| transaction.py | トランザクション管理 | participants |
| block.py | ブロック定義 | なし |
| miner.py | マイニング処理 | block |
| blockchain.py | チェーン管理 | block |
| cli.py | UI統合 | すべて |

### 2.3 データフロー

```
[ユーザー入力]
    ↓
[transaction.py] → transaction-NNNN.json
    ↓
[miner.py] → ブロック生成
    ↓
[blockchain.py] → block-NNNN.json
    ↓
[検証] → 改ざん検知
```

### 2.4 CLI画面遷移

```
起動
 ↓
メインメニュー表示
 ├→ [1] 参加者一覧 → 表示 → メニューに戻る
 ├→ [2] トランザクション作成 → 入力 → 保存 → メニューに戻る
 ├→ [3] 未処理表示 → 表示 → メニューに戻る
 ├→ [4] マイニング → 実行 → 結果表示 → メニューに戻る
 ├→ [5] チェーン表示 → 表示 → メニューに戻る
 ├→ [6] チェーン検証 → 検証 → 結果表示 → メニューに戻る
 ├→ [7] ブロック改ざん → 入力 → 改ざん → メニューに戻る
 └→ [8] 終了 → プログラム終了
```

---

## 3. モジュール詳細設計

### 3.1 participants.py

#### 関数仕様

##### `load_participants() -> list[str]`

**目的:** 参加者リストを読み込み

**入力:** なし

**処理:**
1. `data/participants.json` を開く
2. JSON パース
3. list[str] として返す

**出力:** `["Alice", "Bob", "Carol", "Dave"]`

**例外:**
- FileNotFoundError: ファイルが存在しない

---

##### `is_valid_address(address: str) -> bool`

**目的:** アドレスの有効性を判定

**入力:** `address` - チェック対象のアドレス

**処理:**
1. 長さが3〜5文字かチェック
2. 参加者リストに含まれるかチェック

**出力:** `True` または `False`

**例:**
```python
is_valid_address("Alice")  # True
is_valid_address("Unknown")  # False
is_valid_address("AB")  # False（短すぎる）
```

---

##### `display_participants()`

**目的:** 参加者一覧を見やすく表示

**処理:**
1. ヘッダー表示
2. 参加者を番号付きリストで表示
3. フッター表示

**出力例:**
```
========================================
 登録済み参加者一覧
========================================
1. Alice
2. Bob
3. Carol
4. Dave
========================================
```

---

### 3.2 transaction.py

#### 関数仕様

##### `create_transaction()`

**目的:** 対話的にトランザクションを作成

**処理フロー:**
1. 送信元入力
2. 送信先入力
3. 金額入力
4. バリデーション
   - アドレス存在チェック
   - 金額が正の整数
   - 送信元≠送信先
5. タイムスタンプ付与
6. ファイル保存

**バリデーションエラー:**
- 未登録アドレス → "❌ エラー: 'XXX' は登録されていません"
- 負の金額 → "❌ エラー: 金額は正の整数である必要があります"
- 同一アドレス → "❌ エラー: 送信元と送信先が同じです"

**保存形式:**
```json
{
  "from": "Alice",
  "to": "Bob",
  "amount": 100,
  "timestamp": 1234567890
}
```

**ファイル名:** `transaction-0001.json`, `transaction-0002.json`, ...

---

##### `get_pending_transactions() -> list[dict]`

**目的:** 未処理トランザクション一覧を取得

**処理:**
1. `data/transactions/` を読み込み
2. すべての `.json` ファイルをパース
3. リストとして返す

**出力例:**
```python
[
    {"from": "Alice", "to": "Bob", "amount": 100, "timestamp": 123456},
    {"from": "Bob", "to": "Carol", "amount": 50, "timestamp": 123457}
]
```

---

##### `display_pending_transactions()`

**目的:** 未処理トランザクションを表示

**出力例:**
```
========================================
 未処理トランザクション
========================================
1. Alice → Bob: 100
   タイムスタンプ: 1234567890
2. Bob → Carol: 50
   タイムスタンプ: 1234567891
========================================
```

---

##### `clear_pending_transactions()`

**目的:** 未処理トランザクションをすべて削除

**処理:**
1. `data/transactions/` 内のすべての `.json` を削除

**使用タイミング:** マイニング完了後

---

### 3.3 block.py

#### クラス仕様

##### `class Block`

**属性:**

| 属性 | 型 | 説明 |
|------|-------|------|
| index | int | ブロック番号（0〜） |
| timestamp | int | 作成時刻（UNIXタイム） |
| transactions | list[dict] | トランザクションリスト |
| previous_hash | str | 前のブロックのハッシュ |
| nonce | int | Proof of Work用 |
| hash | str | このブロックのハッシュ |

---

##### `__init__(index, transactions, previous_hash, timestamp=None)`

**目的:** ブロックを初期化

**引数:**
- `index`: ブロック番号
- `transactions`: トランザクションリスト
- `previous_hash`: 前のブロックのハッシュ
- `timestamp`: タイムスタンプ（Noneの場合は現在時刻）

**初期値:**
- `nonce`: 0
- `hash`: "" (空文字列)

---

##### `compute_hash() -> str`

**目的:** ブロックのハッシュを計算

**処理:**
1. ブロックデータを辞書化（hashは含めない）
2. JSON文字列化（sort_keys=True）
3. SHA256でハッシュ化
4. 16進数文字列で返す

**重要:** `hash` 属性は含めない（循環参照を防ぐため）

**実装例:**
```python
def compute_hash(self) -> str:
    block_data = {
        "index": self.index,
        "timestamp": self.timestamp,
        "transactions": self.transactions,
        "previous_hash": self.previous_hash,
        "nonce": self.nonce
    }
    block_string = json.dumps(block_data, sort_keys=True)
    return hashlib.sha256(block_string.encode()).hexdigest()
```

---

##### `to_dict() -> dict`

**目的:** JSON保存用に辞書化

**出力:**
```python
{
    "index": 0,
    "timestamp": 1234567890,
    "transactions": [...],
    "previous_hash": "0000...",
    "nonce": 12345,
    "hash": "00abc..."
}
```

---

##### `from_dict(data: dict) -> Block` (static)

**目的:** 辞書からBlockオブジェクトを復元

**処理:**
1. Blockインスタンス作成
2. nonce, hash を設定
3. 返す

---

### 3.4 miner.py

#### 関数仕様

##### `mine(block: Block, difficulty: str = "00") -> Block`

**目的:** Proof of Work でブロックをマイニング

**引数:**
- `block`: マイニング対象のBlock
- `difficulty`: 難易度条件（例: "00"）

**処理:**
1. nonce = 0 から開始
2. ループ:
   - ハッシュ計算
   - 1000回ごとに進行状況表示
   - ハッシュが difficulty で始まるかチェック
   - 一致したらブロックに設定してreturn
   - 一致しなければ nonce++

**出力:**
- マイニング完了したBlock（hash設定済み）

**表示例:**
```
============================================================
 マイニング開始: Block #1
============================================================
難易度: ハッシュが '00' で始まる必要あり
トランザクション数: 2

試行回数: 0 | 現在のハッシュ: 3a7bd3e2987a...
試行回数: 1,000 | 現在のハッシュ: 8f2e9c1d4b5a...
試行回数: 2,000 | 現在のハッシュ: 1c4f8a3e9d2b...

============================================================
✅ マイニング成功！
============================================================
Nonce: 2,147
Hash: 00a3f5e8c2d1b9e7...
試行回数: 2,148
============================================================
```

---

##### `create_genesis_block() -> Block`

**目的:** ジェネシスブロック（最初のブロック）を作成

**処理:**
1. index=0, previous_hash="0"*64 でBlock作成
2. トランザクション: `{"from": "SYSTEM", "to": "NETWORK", "amount": 0}`
3. difficulty="0" でマイニング
4. 返す

---

### 3.5 blockchain.py

#### 関数仕様

##### `load_blockchain() -> list[Block]`

**目的:** 保存されたすべてのブロックを読み込み

**処理:**
1. `data/blocks/` からすべての `.json` を読み込み
2. Block.from_dict() で復元
3. index順にソートして返す

---

##### `get_last_block() -> Optional[Block]`

**目的:** 最後のブロックを取得

**出力:**
- ブロックが存在: 最後のBlock
- ブロックが0件: None

---

##### `save_block(block: Block)`

**目的:** ブロックをファイルに保存

**ファイル名:** `block-{index:04d}.json`

**例:** `block-0000.json`, `block-0001.json`

---

##### `add_block(block: Block) -> bool`

**目的:** 新しいブロックをチェーンに追加

**検証項目:**
1. ジェネシスブロックの場合: index=0
2. 通常ブロックの場合:
   - index が連続している
   - previous_hash が前のブロックと一致

**出力:**
- 成功: True
- 失敗: False（エラーメッセージ表示）

---

##### `verify_chain() -> bool`

**目的:** チェーン全体の整合性を検証

**検証項目:**
1. 各ブロックのハッシュ再計算
2. 各ブロックの previous_hash 整合性

**出力:**
- すべて正常: True
- 異常検出: False（破損箇所を明示）

**表示例（成功時）:**
```
============================================================
 ブロックチェーン検証
============================================================
検証対象: 3 ブロック

Block #0 を検証中...
   ✅ Block #0 は正常です
Block #1 を検証中...
   ✅ Block #1 は正常です
Block #2 を検証中...
   ✅ Block #2 は正常です

============================================================
✅ ブロックチェーン検証成功！
   すべてのブロックが整合性を保っています
============================================================
```

**表示例（失敗時）:**
```
============================================================
 ブロックチェーン検証
============================================================
検証対象: 3 ブロック

Block #0 を検証中...
   ✅ Block #0 は正常です
Block #1 を検証中...
❌ エラー: Block #1 のハッシュが不正です
   保存されたハッシュ: 00abc123...
   再計算されたハッシュ: 1a2b3c4d...
   → ブロックが改ざんされた可能性があります
============================================================
```

---

##### `display_chain()`

**目的:** チェーン全体を表示

**出力例:**
```
============================================================
 ブロックチェーン
============================================================
総ブロック数: 2

------------------------------------------------------------
Block #0
  タイムスタンプ: 0
  トランザクション数: 1
    1. SYSTEM → NETWORK: 0
  Previous Hash: 0000000000000000...
  Nonce: 42
  Hash: 0a3f5e8c2d1b9e7f...
------------------------------------------------------------
Block #1
  タイムスタンプ: 1234567890
  トランザクション数: 2
    1. Alice → Bob: 100
    2. Bob → Carol: 50
  Previous Hash: 0a3f5e8c2d1b9e7f...
  Nonce: 2147
  Hash: 00c3d4e5f6a7b8c9...
============================================================
```

---

##### `get_chain_status() -> dict`

**目的:** チェーンの現在状態を取得

**出力:**
```python
{
    "block_count": 2,
    "last_block_index": 1,
    "last_block_hash": "00c3d4e5f6a7b8c9..."
}
```

---

### 3.6 cli.py

#### 関数仕様

##### `display_header()`

**出力:**
```
============================================================
 Simple Blockchain - 学習用ブロックチェーン
============================================================
```

---

##### `display_status()`

**出力:**
```
現在のブロック数: 2
未処理トランザクション: 3
```

---

##### `display_menu()`

**出力:**
```
------------------------------------------------------------
[1] 参加者一覧
[2] トランザクション作成
[3] 未処理トランザクション表示
[4] マイニング実行
[5] チェーン表示
[6] チェーン検証
[7] ブロック改ざん（学習用）
[8] 終了
------------------------------------------------------------
```

---

##### `handle_menu_X()` (1〜7)

各メニュー項目に対応する処理を実行

**処理例（handle_menu_4）:**
1. 未処理トランザクションを取得
2. ジェネシスブロック未作成なら作成
3. 新しいブロックを作成
4. マイニング実行
5. チェーンに追加
6. 未処理トランザクションをクリア

---

##### `main()`

**処理:**
1. ループ:
   - ヘッダー表示
   - 状態表示
   - メニュー表示
   - 入力受付
   - 選択に応じた処理実行
   - Enter待機
2. [8]選択で終了

---

## 4. データ設計

### 4.1 participants.json

```json
[
  "Alice",
  "Bob",
  "Carol",
  "Dave"
]
```

### 4.2 transaction-NNNN.json

```json
{
  "from": "Alice",
  "to": "Bob",
  "amount": 100,
  "timestamp": 1234567890
}
```

### 4.3 block-NNNN.json

```json
{
  "index": 1,
  "timestamp": 1234567890,
  "transactions": [
    {
      "from": "Alice",
      "to": "Bob",
      "amount": 100,
      "timestamp": 1234567890
    }
  ],
  "previous_hash": "0a3f5e8c2d1b9e7f...",
  "nonce": 2147,
  "hash": "00c3d4e5f6a7b8c9..."
}
```

---

## 5. UI設計

### 5.1 起動画面

```
============================================================
 Simple Blockchain - 学習用ブロックチェーン
============================================================

現在のブロック数: 0
未処理トランザクション: 0

------------------------------------------------------------
[1] 参加者一覧
[2] トランザクション作成
[3] 未処理トランザクション表示
[4] マイニング実行
[5] チェーン表示
[6] チェーン検証
[7] ブロック改ざん（学習用）
[8] 終了
------------------------------------------------------------

選択してください:
```

### 5.2 視覚的ガイドライン

**区切り線:**
- ヘッダー/フッター: `"="*60`
- セクション区切り: `"-"*60`

**記号:**
- 成功: ✅
- エラー: ❌
- 警告: ⚠️
- ヒント: 💡

**インデント:**
- 項目表示: 2スペース
- サブ項目: 4スペース

---

## 付録: 実装チェックリスト

### Phase 1: セットアップ
- [ ] ディレクトリ構造作成
- [ ] participants.json 作成
- [ ] __init__.py 作成

### Phase 2: 基本モジュール
- [ ] participants.py 実装
- [ ] transaction.py 実装
- [ ] 単体テスト実行

### Phase 3: ブロック機能
- [ ] block.py 実装
- [ ] miner.py 実装
- [ ] 単体テスト実行

### Phase 4: チェーン管理
- [ ] blockchain.py 実装
- [ ] 検証機能実装
- [ ] 単体テスト実行

### Phase 5: CLI統合
- [ ] cli.py 実装
- [ ] 統合テスト実行
- [ ] 改ざん検知テスト実行

---

**文書バージョン:** 1.0  
**最終更新日:** 2026-02-14
